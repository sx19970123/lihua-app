import { defaultConfig } from '../config';
export const defaultWatermarkProps = defaultConfig.watermark;
export const defaultWatermarkFont = {
    color: 'rgba(0,0,0,.15)',
    fontSize: 14,
    fontWeight: 'normal',
    fontFamily: 'sans-serif',
    fontStyle: 'normal',
    textAlign: 'center',
};
